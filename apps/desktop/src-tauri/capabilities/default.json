{
  "$schema": "../gen/schemas/desktop.json",
  "identifier": "default",
  "description": "Serial Tool desktop permissions",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-set-theme",
    "dialog:allow-open",
    "dialog:allow-save"
  ]
}
